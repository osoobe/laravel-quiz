<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

// Optional stub for attaching a scoped quiz to a host entity (event, course, cohort, ...).
// Copy this into your app's migrations, rename the table, then implement
// Osoobe\Quiz\Contracts\QuizScope on that model and use the Osoobe\Quiz\Support\HasQuiz trait.

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('{{table}}', function (Blueprint $table) {
            $table->boolean('quiz_mode_enabled')->default(false);
            $table->foreignUuid('quiz_id')->nullable()->constrained('quizzes')->nullOnDelete();
        });
    }

    public function down(): void
    {
        Schema::table('{{table}}', function (Blueprint $table) {
            $table->dropConstrainedForeignId('quiz_id');
            $table->dropColumn('quiz_mode_enabled');
        });
    }
};
